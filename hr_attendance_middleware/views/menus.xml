<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Root menu di bawah Attendances -->
    <menuitem
        id="menu_hr_attendance_middleware_root"
        name="HR-Attendance"
        parent="hr_attendance.menu_hr_attendance_root"
        sequence="85"/>

    <!-- Sub-menu: Mesin Absensi -->
    <menuitem
        id="menu_hr_attendance_device"
        name="Mesin Absensi"
        parent="menu_hr_attendance_middleware_root"
        action="hr_attendance_device_action"
        sequence="10"/>

    <!-- Sub-menu: Log Raw -->
    <menuitem
        id="menu_hr_attendance_raw_log"
        name="Log Raw Data"
        parent="menu_hr_attendance_middleware_root"
        action="hr_attendance_raw_log_action"
        sequence="20"/>

    <!-- Sub-menu: Riwayat Sync -->
    <menuitem
        id="menu_hr_attendance_sync_history"
        name="Riwayat Sinkronisasi"
        parent="menu_hr_attendance_middleware_root"
        action="hr_attendance_sync_history_action"
        sequence="30"/>

    <!-- Sub-menu: Konfigurasi (links to general Settings; middleware settings appear in Attendances section) -->
    <menuitem
        id="menu_hr_attendance_middleware_config"
        name="Konfigurasi"
        parent="menu_hr_attendance_middleware_root"
        action="base_setup.action_general_configuration"
        sequence="99"/>

</odoo>
