<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ===== Extend project.task form view — add Week No. field ===== -->
    <record id="view_task_form_scurve" model="ir.ui.view">
        <field name="name">project.task.form.scurve</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='planned_hours']" position="after">
                <field name="x_week_number"
                       string="Week No."
                       invisible="not project_id"
                       help="Week number for S-Curve grouping (1–52). Set 0 to exclude from S-Curve."/>
            </xpath>
        </field>
    </record>

    <!-- ===== Extend project.task list view — add Week No. column ===== -->
    <record id="view_task_tree_scurve" model="ir.ui.view">
        <field name="name">project.task.tree.scurve</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_tree2"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='planned_hours']" position="after">
                <field name="x_week_number" string="Week No." optional="show"/>
            </xpath>
        </field>
    </record>

</odoo>
